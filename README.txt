# integration-by-trapezium-rule

Integrates an equation of a line between two points:

example input:

integrate(lambda x: x**2, [0,5])

this will integrate an x-squared line between the values 0 and 5.